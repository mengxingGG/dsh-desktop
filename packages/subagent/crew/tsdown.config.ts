import { defineConfig } from 'tsdown'

/** Build the native Crew runtime and invariant companion from emitted ESM. */
export default defineConfig([
  {
    entry: ['lib/types/index.js', 'lib/types/preferences.js'],
    outDir: 'lib',
    format: ['esm'],
    platform: 'node',
    target: 'es2024',
    fixedExtension: false,
    dts: false,
    clean: false,
  },
  {
    entry: ['lib/types/invariant.js'],
    outDir: 'lib',
    format: ['esm'],
    platform: 'node',
    target: 'es2024',
    fixedExtension: false,
    dts: false,
    clean: false,
  },
])
