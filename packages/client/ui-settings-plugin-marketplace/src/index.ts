/** Host loader entry for the plugin-marketplace browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the marketplace tab. */
export function apply(): void {}
